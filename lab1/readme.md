# To-Do List Cli

the to-do contains a title and status

## Commands

- add 'title'
- remove 'title'
- edit 'title' --title, -t 'new title' or --status, -s 'new status'
- ls --status, -s 'status' (you can list all or list per status)
